# HashSet equals & hashCode

## Task
Add several identical `User` objects to a `HashSet` and make sure only **one** remains in the set.  

### Note
Override `equals()` and `hashCode()` in the `User` class so duplicates are recognized.  

### Expected output
```
Passed!
```
